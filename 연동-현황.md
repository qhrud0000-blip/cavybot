# 프로젝트별 연동·실행 현황 (API / 기획)

> "제대로 실행될 때까지" 추적하는 체크보드입니다.
> ✅ 완료(코드/문서로 동작) · 🟡 키만 넣으면 동작 · 🔴 본인 액션 필요(가입/승인)
> 최종 갱신: 2026-06-14

## 한눈에

| 프로젝트 | 핵심 연동 | 상태 | 다음 액션(본인) |
|----------|-----------|:---:|------|
| 대시보드 | 정적 웹앱(서버 불필요) | ✅ | 로컬서버로 열기 |
| 대시보드 파일보관함 | IndexedDB(브라우저) | ✅ | 없음 |
| 오늘의 추천 | daily.json + GitHub Actions | ✅ | main 병합 시 자동 |
| 콘텐츠/프롬프트 | 문서(복붙형) | ✅ | 없음 |
| 쿠팡 자동화 | Firecrawl+Gemini+n8n | 🟡 | 키 발급 후 .env |
| 저장(시트/노션) | n8n GoogleSheets/Notion | 🟡 | Credentials 연결 |
| 쿠팡파트너스 | 딥링크/리포트 | 🔴 | 가입·채널 승인 |
| 영상/이미지 | Replicate/fal(선택) | 🟡 | 무료 한도만, 기본 미사용 |

## 단계별 "실행" 정의 & 점검법

### ✅ 지금 바로 실행 확인 가능 (키 불필요)
- **대시보드**: `cd dashboard && python3 -m http.server 8080` → http://localhost:8080
  - 드래그 이동 / ＋📎✎⧉🗑 / MCP 추천 모달 / 파일 업로드·다운로드·문서생성 동작.
- **오늘의 추천 생성**: `node scripts/update-daily.mjs` → `dashboard/data/daily.json` 갱신.

### 🟡 키만 넣으면 실행 (무료, 카드 X)
1. `cp automation/.env.example automation/.env` 후 키 입력
   - `FIRECRAWL_API_KEY` (firecrawl.dev, 무료)
   - `GEMINI_API_KEY` (aistudio.google.com, 무료)
2. 연결 테스트: `node scripts/test-connections.mjs`  ← 키가 살아있는지 ping
3. n8n 실행 후 `automation/n8n-workflow*.json` import → Execute 1회.

### 🔴 본인만 할 수 있는 가입/승인
- 쿠팡파트너스 가입 → 채널 등록 → 승인 → 키 발급 (partners.coupang.com)
- 구글/노션 저장 쓰면 각 OAuth/통합 토큰 발급 후 n8n Credentials 연결.

## 진행 로그
- 2026-06-14: 대시보드·파일보관함·프롬프트·n8n(기본/시트/노션)·일일추천·연결테스트 스크립트 구축 완료. 가입·키 입력만 남음.
